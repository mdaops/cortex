---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: argo
spec:
  interval: 1h
  url: https://argoproj.github.io/argo-helm
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: argocd
spec:
  interval: 30m
  targetNamespace: argo-system
  install:
    createNamespace: true
  kubeConfig:
    secretRef:
      name: cluster-kubeconfig
  chart:
    spec:
      chart: argo-cd
      version: "9.2.4"
      sourceRef:
        kind: HelmRepository
        name: argo
      interval: 12h
  valuesFrom:
    - kind: ConfigMap
      name: argocd-crossplane-health
  values:
    configs:
      cm:
        application.resourceTrackingMethod: annotation
        resource.exclusions: |
          - apiGroups:
              - "*"
            kinds:
              - ProviderConfigUsage
        accounts.crossplane: apiKey
      rbac:
        policy.csv: |
          p, role:crossplane, applications, *, */*, allow
          p, role:crossplane, projects, *, *, allow
          p, role:crossplane, repositories, *, *, allow
          g, crossplane, role:crossplane
      params:
        server.insecure: true
    controller:
      env:
        - name: ARGOCD_K8S_CLIENT_QPS
          value: "300"
    server:
      service:
        type: ClusterIP
    extraObjects:
      - apiVersion: v1
        kind: ServiceAccount
        metadata:
          name: argocd-token-generator
          namespace: argo-system
      - apiVersion: rbac.authorization.k8s.io/v1
        kind: Role
        metadata:
          name: argocd-token-generator
          namespace: argo-system
        rules:
          - apiGroups: [""]
            resources: ["secrets"]
            verbs: ["get"]
      - apiVersion: rbac.authorization.k8s.io/v1
        kind: RoleBinding
        metadata:
          name: argocd-token-generator
          namespace: argo-system
        subjects:
          - kind: ServiceAccount
            name: argocd-token-generator
            namespace: argo-system
        roleRef:
          kind: Role
          name: argocd-token-generator
          apiGroup: rbac.authorization.k8s.io
      - apiVersion: rbac.authorization.k8s.io/v1
        kind: Role
        metadata:
          name: argocd-token-generator
          namespace: crossplane-system
        rules:
          - apiGroups: [""]
            resources: ["secrets"]
            verbs: ["get", "create", "patch"]
      - apiVersion: rbac.authorization.k8s.io/v1
        kind: RoleBinding
        metadata:
          name: argocd-token-generator
          namespace: crossplane-system
        subjects:
          - kind: ServiceAccount
            name: argocd-token-generator
            namespace: argo-system
        roleRef:
          kind: Role
          name: argocd-token-generator
          apiGroup: rbac.authorization.k8s.io
      - apiVersion: batch/v1
        kind: Job
        metadata:
          name: argocd-crossplane-token
          namespace: argo-system
          annotations:
            helm.sh/hook: post-install,post-upgrade
            helm.sh/hook-weight: "10"
            helm.sh/hook-delete-policy: before-hook-creation
        spec:
          ttlSecondsAfterFinished: 300
          backoffLimit: 5
          template:
            spec:
              serviceAccountName: argocd-token-generator
              restartPolicy: OnFailure
              securityContext:
                seccompProfile:
                  type: RuntimeDefault
              volumes:
                - name: token
                  emptyDir: {}
              initContainers:
                - name: get-token
                  image: docker.io/curlimages/curl:8.11.1
                  securityContext:
                    allowPrivilegeEscalation: false
                    runAsNonRoot: true
                    runAsUser: 1000
                    capabilities:
                      drop: ["ALL"]
                  resources:
                    requests:
                      memory: 32Mi
                    limits:
                      memory: 64Mi
                  volumeMounts:
                    - name: token
                      mountPath: /token
                  env:
                    - name: ARGOCD_ADMIN_PASSWORD
                      valueFrom:
                        secretKeyRef:
                          name: argocd-initial-admin-secret
                          key: password
                  command: ["/bin/sh", "-c"]
                  args:
                    - |
                      set -eu
                      SERVER="http://argo-system-argocd-server.argo-system.svc"
                      SESSION=$(curl -sf "$SERVER/api/v1/session" \
                        -H "Content-Type: application/json" \
                        -d "{\"username\":\"admin\",\"password\":\"$ARGOCD_ADMIN_PASSWORD\"}")
                      TOKEN=$(echo "$SESSION" | sed -n 's/.*"token":"\([^"]*\)".*/\1/p')
                      [ -z "$TOKEN" ] && exit 1
                      RESP=$(curl -sf "$SERVER/api/v1/account/crossplane/token" \
                        -H "Authorization: Bearer $TOKEN" \
                        -H "Content-Type: application/json" \
                        -d '{"name":"crossplane-provider"}')
                      echo "$RESP" | sed -n 's/.*"token":"\([^"]*\)".*/\1/p' > /token/value
              containers:
                - name: create-secret
                  image: docker.io/alpine/k8s:1.32.0
                  securityContext:
                    allowPrivilegeEscalation: false
                    runAsNonRoot: true
                    runAsUser: 1000
                    capabilities:
                      drop: ["ALL"]
                  resources:
                    requests:
                      memory: 32Mi
                    limits:
                      memory: 64Mi
                  volumeMounts:
                    - name: token
                      mountPath: /token
                      readOnly: true
                  command: ["/bin/sh", "-c"]
                  args:
                    - |
                      kubectl create secret generic argocd-credentials \
                        --namespace crossplane-system \
                        --from-literal=authToken="$(cat /token/value)" \
                        --dry-run=client -o yaml | kubectl apply -f -
